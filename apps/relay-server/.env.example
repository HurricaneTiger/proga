# Port for the HTTP and WebSocket relay server
RELAY_PORT=3000

# WebSocket endpoint path
WS_PATH=/ws

# Maximum number of simultaneous rooms
MAX_ROOMS=100

# Room timeout in milliseconds (default: 1 hour)
# Rooms are automatically cleaned up after this duration
ROOM_TIMEOUT_MS=3600000
